{
  "name": "terrawalk",
  "private": true,
  "scripts": {
    "build": "cd client && npm ci && npm run build && cd ../server && npm ci && npx tsc",
    "start": "cd server && node dist/index.js",
    "migrate": "cd server && npx tsx src/migrate.ts"
  }
}
